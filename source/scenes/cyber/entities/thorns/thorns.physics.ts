export default function() {
  
  //   const perm = permutations(
  //     entity.mesh.box.a,
  //     entity.mesh.box.b,
  //   ).map(x => vec3.transformMat4([0,0,0], x, model)) as Array<Array<number>>;

  //   const body = RigidBodyDesc.fixed();

  //   const collider = scene.world.createCollider(
  //     ColliderDesc.convexMesh(new Float32Array(perm.flat()))!, 
  //     scene.world.createRigidBody(body)
  //   );

  //   const groupFlag = 0b0000_0000_0000_1000_0000_0000_0000_0001;

  //   collider.setCollisionGroups(groupFlag);
  //   collider.setSolverGroups(groupFlag);

}